telegram:
  python:
    server:
      url: http://localhost:8081
    timeout:
      connect: 50000
      read: 600000

server:
  port: 8080

spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:  325605549031-9orntk5t3vimulmrk58iherus5d6m490.apps.googleusercontent.com
            client-secret:  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
            scope:
              - openid
              - email
              - profile
              - https://www.googleapis.com/auth/gmail.send
            access-type: offline
            prompt: consent

        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://openidconnect.googleapis.com/v1/userinfo

  liquibase:
    enabled: false  # Отключаем автоконфигурацию!

  h2:
    console:
      enabled: false

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Europe/Moscow
    serialization:
      write-dates-as-timestamps: false

  datasource:
    url: jdbc:postgresql://localhost:5432/krrg
    driver-class-name: org.postgresql.Driver
    username: krrg
    password: H.hbr2123

  jpa:
    defer-datasource-initialization: false
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

  sql:
    init:
      mode: never  # Отключаем, чтобы не мешал Liquibase

  ai:
    openai:
      api-key: dummy-key  # Временно, чтобы не падало при старте
      chat:
        enabled: false    # <-- ОТКЛЮЧАЕМ автоконфигурацию